// eslint-disable-next-line react/display-name
export default props => (
    <div>
        <nav>
            <ul>....</ul>
        </nav>
        <main>
            {props.content}
        </main>
    </div>
)
